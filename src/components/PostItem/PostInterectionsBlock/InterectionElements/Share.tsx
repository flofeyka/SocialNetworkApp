import { Button } from "@nextui-org/react";
import { FC } from "react";

const Share: FC = () => {
  return (
    <span>
      <Button variant="faded" radius="full" size="sm">Поделиться</Button>
    </span>
  );
};

export default Share;
